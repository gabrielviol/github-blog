import styled from "styled-components";

export const Header = styled.header`
    display: flex;
    position: absolute;
    justify-content: center;
    width: 100%;

    img {
        display: flex;
        height: 296px;
    }
`