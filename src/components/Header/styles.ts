import styled from "styled-components";

export const Container = styled.div`
    overflow: hidden;
        
        img{
            display: block;
             margin-left: auto;
             margin-right: auto;
            }
    
`